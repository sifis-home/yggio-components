const DEFAULTS = {
  containerButtonSize: 20,
  containerButtonMargin: '0px',
  containerButtonPadding: '0px',
  loadingColor: 'transparent',
  selectedColor: 'green',
  unselectedColor: '#888',
  disabledOpacity: 0.6,
  enabledOpacity: 1,
};

export {
  DEFAULTS,
};
