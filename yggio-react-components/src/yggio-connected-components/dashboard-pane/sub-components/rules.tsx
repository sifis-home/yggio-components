/*
import ContainerBox from '../../../components/container-box';

################# TODO START: Implement support for rules #################
const Rules = props => {
  const rulesQuery = rulesApi.useRulesQuery();
  return (
    <ContainerBox margin={'10px 0 10px'} width={'100%'} height={'300px'}>
        <h4>Rules</h4>
        <ul>
          {_.map(rulesQuery?.data, rule => (
            <li>{rule.name}</li>
          ))}
        </ul>
      </ContainerBox>
  )
};

export default Rules;
################# TODO START: Implement support for rules #################
*/
